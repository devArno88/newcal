// import { E_Fetches } from "../interfaces";
// import { fetcher, fetcherPrep } from "../utils";

export const getDevelopmentIssues = async (): Promise<any> => {
    // try {
    //     return await fetcher(`/api/chat/${chatID}`, fetcherPrep({ method: E_Fetches.get }));
    // } catch (err) {
    //     console.error(err);
    // }
};
